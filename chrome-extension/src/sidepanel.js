/* ==========================================================
   Side Panel – Entry point
   Imports the init module which bootstraps everything.
   ========================================================== */

import "./init.js";
